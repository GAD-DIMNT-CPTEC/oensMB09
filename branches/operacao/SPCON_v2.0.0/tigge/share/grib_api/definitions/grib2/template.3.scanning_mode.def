flags[1] scanningMode 'grib2/3.4.table' :optional;

# Not flagbit numbers 7 to 0, while wmo is 1 to 8
flagbit iScansNegatively(scanningMode,7);
flagbit jScansPositively(scanningMode,6);
flagbit jPointsAreConsecutive(scanningMode,5);
flagbit alternativeRowScanning(scanningMode,4) = 0 : optional,edition_specific;

flagbit scanningMode5(scanningMode,3) = 0: read_only;
flagbit scanningMode6(scanningMode,2) = 0: read_only;
flagbit scanningMode7(scanningMode,1) = 0: read_only;
flagbit scanningMode8(scanningMode,0) = 0: read_only;

#meta changeScanningDirection
#     change_scanning_direction( values,
#                                iScansNegatively,
#                                jScansPositively,
#                                jPointsAreConsecutive,
#                                alternativeRowScanning) : edition_specific;
